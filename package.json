{
  "name": "medi-guru",
  "version": "1.0.0",
  "description": "Medical AI Chatbot",
  "scripts": {
    "dev": "echo \"Run frontend with live server and backend separately\"" ,
    "build": "echo \"No build required for static site\""
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@vercel/node": "^4.0.0"
  }
}
